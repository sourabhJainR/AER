# GitHub integration

This directory contains the metadata and documentation required for `.aer` repository recognition. The canonical AER file extension is `.aer`; binary payloads use `.aerb`.

The long-term goal is native GitHub syntax/rendering support once AER is registered with the relevant GitHub language-definition workflow. Until then, repository consumers can still use the CLI and editor integrations.
